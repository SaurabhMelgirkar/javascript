// let obj={
    // name:"ram",
    // add:"pune",
    // mob:5677889990,
    // func:()=>{
        // console.log(obj.name);
        // this.name will work with normal function but it will not work with arrow function
        // 
    // }
// }
// obj.func()


let arr=[10,20,30]
console.log(typeof(arr));
// print(typeof(arr))
arr.forEach((a,i,w)=>{console.log(a,i,w);
})
